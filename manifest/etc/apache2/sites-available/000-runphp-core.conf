# Default charset
AddDefaultCharset UTF-8

# This can cause resource exhaustion
KeepAlive Off

# We can trust Google Cloud Run's "X-Forwarded-For" header
RemoteIPHeader X-Forwarded-For

# Don't expose the server version
ServerSignature Off
ServerTokens Prod